const { Debits } = require('../../database/models');

class DebitsService {
  async store(debit) {
    // Store debit
  }

  async destroy({ id }) {
    // Delete debit
  }
}

module.exports = new DebitsService();
