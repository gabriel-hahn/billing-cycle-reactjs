const { Credits } = require('../../database/models');

class CreditsService {
  async store(credit) {
    // Store credit
  }

  async destroy({ id }) {
    // Delete credit
  }
}

module.exports = new CreditsService();
